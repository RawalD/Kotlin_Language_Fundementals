
fun main() {
    //Add
    var one = 5
    var two = 10
    var result = one + two
    println(result)

    //Sub
    one = 10
    two = 5
    result = one - two
    println(result)

    //Multiply
    one = 10
    two = 5
    result = one * two
    println(result)

    //Divide
    one = 10
    two = 5
    result = one / two
    println(result)

    //Mod
    one = 10
    two = 5
    result = one % two
    println(result)

    //Increment
    one++

    //Decrement
    two--

    //Assign and do math
    one +=5
    one -=2
    one /=2
    one *= 3
    one %= 2

}